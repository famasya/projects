const assert = require('assert');
const datasearch = require('../index.js');

describe('datasearch', function () {
  it('has a test', function () {
    assert(false, 'datasearch should have a test');
  });
});
